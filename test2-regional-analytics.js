(()=>{'use strict';
const AUTH='ap207-auth-profile-v1',PROPS='ap207-dashboard-properties-v1',USERS='system-control-test2-users-v1',INVITES='stay-control-invitations-v1',SUITE='system-control-test2-suite-v1';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||'null')||f}catch{return f}};const profile=()=>read(AUTH,{})?.profile||{};const norm=v=>String(v||'Não informado').trim()||'Não informado';
function css(){if(document.getElementById('t2RegionalAnalyticsCss'))return;const s=document.createElement('style');s.id='t2RegionalAnalyticsCss';s.textContent=`#t2Analytics{background:transparent!important;border:0!important;box-shadow:none!important;padding:0!important}.t2ra{display:grid;gap:16px}.t2ra-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;flex-wrap:wrap}.t2ra-head h2{margin:0 0 5px;font-size:30px}.t2ra-head p{margin:0;color:#64748b}.t2ra-actions{display:flex;gap:8px;flex-wrap:wrap}.t2ra-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.t2ra-card,.t2ra-tablecard{background:#fff;border:1px solid #e5e7eb;border-radius:16px;padding:16px}.t2ra-card strong{display:block;font-size:25px;margin-top:5px}.t2ra-card span{color:#64748b;font-size:13px}.t2ra-tablewrap{overflow:auto}.t2ra-table{width:100%;border-collapse:collapse;min-width:760px}.t2ra-table th,.t2ra-table td{text-align:left;padding:11px 12px;border-bottom:1px solid #eef1f5}.t2ra-table th{background:#f8fafc;color:#475569;font-size:13px}.t2ra-note{padding:12px 14px;background:#eef4ff;border:1px solid #c7d7fe;border-radius:12px;color:#334155;font-size:13px}.t2ra-bar{height:9px;background:#eef2ff;border-radius:999px;overflow:hidden;min-width:100px}.t2ra-bar i{display:block;height:100%;background:#6d28d9;border-radius:inherit}@media(max-width:850px){.t2ra-kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:560px){.t2ra-kpis{grid-template-columns:1fr}.t2ra-actions{width:100%}.t2ra-actions button{flex:1}}`;document.head.append(s)}
function properties(){return (read(PROPS,{properties:[]}).properties||[]).filter(Boolean)}function users(){return [...(read(USERS,[])||[]),...(read(INVITES,[])||[])].filter(Boolean)}function audit(){return (read(SUITE,{}).audit||[]).filter(Boolean)}
function locationOfProperty(p){return{country:norm(p.country||'Brasil'),state:norm(p.state||p.region),city:norm(p.city)}}
function buildData(){const ps=properties(),us=users(),logs=audit(),pmap=new Map(ps.map(p=>[String(p.id),p])),map=new Map();const add=(country,state,city,type)=>{const key=[country,state,city].join('|'),r=map.get(key)||{country,state,city,properties:0,registeredUsers:0,accesses:0};r[type]++;map.set(key,r)};ps.forEach(p=>{const l=locationOfProperty(p);add(l.country,l.state,l.city,'properties')});us.forEach(u=>{const country=norm(u.country||u.pais||'Não informado'),state=norm(u.state||u.region||u.estado),city=norm(u.city||u.cidade);if(country!=='Não informado'||state!=='Não informado'||city!=='Não informado')add(country,state,city,'registeredUsers')});logs.forEach(x=>{let country=norm(x.country||x.pais),state=norm(x.state||x.region||x.estado),city=norm(x.city||x.cidade);if(country==='Não informado'&&x.propertyId&&pmap.has(String(x.propertyId))){const l=locationOfProperty(pmap.get(String(x.propertyId)));country=l.country;state=l.state;city=l.city}if(country!=='Não informado'||state!=='Não informado'||city!=='Não informado')add(country,state,city,'accesses')});const rows=[...map.values()].sort((a,b)=>b.accesses-a.accesses||b.properties-a.properties||a.country.localeCompare(b.country));return{rows,properties:ps.length,users:us.length,accesses:logs.length,countries:new Set(rows.filter(x=>x.country!=='Não informado').map(x=>x.country)).size}}
function reportText(d){const top=d.rows.slice(0,10).map((r,i)=>`${i+1}. ${r.city} / ${r.state} / ${r.country}: ${r.accesses} acessos registrados, ${r.properties} propriedades, ${r.registeredUsers} usuários cadastrados`).join('\n');return `Stay in Control — Relatório geográfico para publicidade\n\nPaíses com presença: ${d.countries}\nPropriedades cadastradas: ${d.properties}\nUsuários cadastrados: ${d.users}\nEventos de acesso/uso registrados: ${d.accesses}\n\nPrincipais regiões:\n${top||'Sem dados geográficos suficientes.'}\n\nObservação: os acessos são associados à região registrada no sistema ou à região da propriedade vinculada quando não existe geolocalização direta do dispositivo.`}
async function share(d){const text=reportText(d);try{if(navigator.share){await navigator.share({title:'Relatório geográfico — Stay in Control',text});return}await navigator.clipboard.writeText(text);alert('Relatório copiado para compartilhar.')}catch{}}
function render(){if(profile().role!=='super_admin')return;const root=document.getElementById('t2Analytics');if(!root)return;css();const d=buildData(),max=Math.max(1,...d.rows.map(x=>x.accesses));root.innerHTML='';const wrap=document.createElement('div');wrap.className='t2ra';wrap.innerHTML=`<div class="t2ra-head"><div><h2>Analíticos</h2><p>Visão geográfica para acompanhar presença regional e apoiar a venda de publicidade.</p></div><div class="t2ra-actions"><button type="button" class="button button-primary" data-share>↗ Compartilhar relatório</button></div></div><div class="t2ra-kpis"><article class="t2ra-card"><span>Países com presença</span><strong>${d.countries}</strong></article><article class="t2ra-card"><span>Propriedades cadastradas</span><strong>${d.properties}</strong></article><article class="t2ra-card"><span>Usuários cadastrados</span><strong>${d.users}</strong></article><article class="t2ra-card"><span>Eventos de acesso/uso</span><strong>${d.accesses}</strong></article></div><div class="t2ra-note"><strong>Relatório comercial:</strong> mostra país, estado/região e cidade com base nos dados cadastrados e no histórico de uso ligado às propriedades. Quando no futuro houver geolocalização direta do acesso, ela poderá substituir essa associação por localização real do dispositivo.</div><section class="t2ra-tablecard"><h3>País / região / cidade</h3><div class="t2ra-tablewrap"><table class="t2ra-table"><thead><tr><th>País</th><th>Estado / região</th><th>Cidade</th><th>Acessos registrados</th><th>Propriedades</th><th>Usuários cadastrados</th><th>Representatividade</th></tr></thead><tbody></tbody></table></div></section>`;const body=wrap.querySelector('tbody');if(!d.rows.length){const tr=document.createElement('tr');tr.innerHTML='<td colspan="7">Ainda não existem dados geográficos suficientes.</td>';body.append(tr)}else d.rows.forEach(r=>{const tr=document.createElement('tr'),pct=Math.round(r.accesses/max*100);tr.innerHTML=`<td>${r.country}</td><td>${r.state}</td><td>${r.city}</td><td><strong>${r.accesses}</strong></td><td>${r.properties}</td><td>${r.registeredUsers}</td><td><div class="t2ra-bar"><i style="width:${pct}%"></i></div></td>`;body.append(tr)});wrap.querySelector('[data-share]').onclick=()=>share(d);root.append(wrap)}
function boot(){window.Test2RegionalAnalytics={render};window.addEventListener('stay:unified-navigation',e=>{if(e.detail?.route==='analytics')render()});window.addEventListener('storage',e=>{if([PROPS,USERS,INVITES,SUITE].includes(e.key))render()});if(document.body.dataset.t2Route==='analytics')render()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();